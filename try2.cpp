#include<iostream>

using namespace std;

int main(){
    const int fibo = 13;
    int count = 5, length=0, i, inside_sum;
    char arr1[fibo][5];
    
    while (inside_sum <= count){
        arr[i]
    }
}